@model IQueryable<NewsItem>

<div>
    @if (Model.Any())
    {
        <ul class="big-image-list">
            @foreach (NewsItem entity in Model.OrderByDescending(x => x.DateAdded))
            {
                <li>
                    <h2>@Html.Raw(entity.Title)</h2>
                    <div>@Html.Raw(entity.Text)</div>
                </li>
                <hr />
            }
        </ul>
    }
</div>